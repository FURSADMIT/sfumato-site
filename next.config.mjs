/** @type {import('next').NextConfig} */
const nextConfig = {
  // Статический экспорт: `next build` кладёт готовый сайт в папку out/
  // (HTML/CSS/JS без Node-сервера) — под обычный хостинг Рег.ру
  output: "export",
  // На выходе .../index.html в папках — чтобы чистые URL открывались на Apache без донастройки
  trailingSlash: true,
  // Разрешаем открывать дев-сервер с телефона по IP мака в локальной сети
  // (иначе Next блокирует JS-чанки с 403 и на устройстве не работает ничего скриптового)
  // важно: «*» закрывает ровно один сегмент адреса, поэтому шаблоны должны быть четырёхсегментными
  allowedDevOrigins: [
    "192.168.*.*",
    "172.16.*.*", "172.17.*.*", "172.18.*.*", "172.19.*.*",
    "172.20.*.*", "172.21.*.*", "172.22.*.*", "172.23.*.*",
    "10.*.*.*",
  ],
};

export default nextConfig;
